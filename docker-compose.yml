services:
  web:
    build: .
    restart: always
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      DJANGO_DB_NAME: data/db.sqlite3
    volumes:
      - ./mysite/data:/app/mysite/data
      - ./mysite/uploads:/app/mysite/uploads
